# miniCRM REST API (Basic auth: username = SystemId, password = API key)
# Phase 1: used only when you run API smoke tests yourself; the MCP stub does not call the API yet.
# Phase 2: required for real tool handlers.
MINICRM_SYSTEM_ID=
MINICRM_API_KEY=
MINICRM_BASE_URL=https://r3.minicrm.hu
